<script setup>
import { computed } from 'vue'

const props = defineProps({
  variant: {
    type: String,
    default: 'default',
    validator: (value) => ['default', 'success', 'warning', 'danger', 'info'].includes(value)
  },
  size: {
    type: String,
    default: 'default',
    validator: (value) => ['sm', 'default'].includes(value)
  }
})

const variantClasses = computed(() => ({
  default: 'bg-gray-100 text-gray-800',
  success: 'bg-green-100 text-green-800',
  warning: 'bg-yellow-100 text-yellow-800',
  danger: 'bg-red-100 text-red-800',
  info: 'bg-blue-100 text-blue-800'
}))

const sizeClasses = computed(() => ({
  sm: 'px-2 py-0.5 text-xs',
  default: 'px-2.5 py-0.5 text-sm'
}))
</script>

<template>
  <span :class="[variantClasses[variant], sizeClasses[size], 'inline-flex items-center font-medium rounded-full']">
    <slot />
  </span>
</template>
